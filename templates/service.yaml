apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-security-service.fullname" . }}
  labels:
    {{- include "data-security-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "data-security-service.selectorLabels" . | nindent 4 }}
